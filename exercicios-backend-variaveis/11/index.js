
const r = 3;
const d = 2 * r;

const v = (4 / 3 * (Math.pow(d , 3)) * Math.PI).toFixed(2)

const resultado = (v / Math.PI).toFixed()



console.log(`O volume de uma esfera de Diametro ${d} é ${resultado} PI`);