const x1 = 1
const x2 = 1
const y1 = 1
const y2 = 4

let distancia = Math.sqrt(Math.pow((x2 - x1) + (y2 - y1), 2))

let mensagem = `A distancia entre o os pontos é de ${distancia}`
console.log(mensagem);
